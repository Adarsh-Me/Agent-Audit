# AgentAudit backend — pinned per TECHSPEC §2 (lockfile policy: exact pins)
fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy[asyncio]==2.0.41
aiosqlite==0.21.0
asyncpg==0.30.0
pydantic==2.11.5
pydantic-settings==2.9.1
httpx==0.28.1
PyYAML==6.0.2
python-multipart==0.0.20
# Remote MCP (streamable HTTP at /mcp) — 1.x line: 2.x forces starlette>=1.0,
# which fastapi==0.115.12 forbids (<0.47). Verified compatible pair locally.
mcp==1.29.1
# Sarvam AI SDK — OpenAI-style chat.completions; base_url/api_key_env drives the
# existing routing, so the engine never imports sarvamai directly (no SDK
# upgrade can break a running audit).
sarvamai==0.1.31a4

# dev / test
pytest==8.3.5
pytest-asyncio==0.26.0
ruff==0.11.13
